/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

const gitDefinition = 'Git(not to be confused with githib) is a version control system that allous individuals and teams to track changes to code projects, prevents bugs and loss of progress, and allows you to vies the history of a project in snapshots.'

//CODE HERE

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

const gitHubDefinition = "Github(not to be confused with git), that can be used in conjuction with git, while working by yourself and/or others to put up different versions/stages of projects to used by yourself or others. It is a cloud service and open source so it prevents loss of data and allows large and unlimited contributions to various projects, whether they be small personal ones, or much larger coorperate ones."

//CODE HERE

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

const gitInitDefinition = "Used to initialize(or 'create') a new git repository or reinitialize an existing one."

//CODE HERE

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

const gitCloneDefinition = "Primarily used to point to an existing repo and make a clone or copy of that repo at in a new directory, at another location"

//CODE HERE

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

const gitStatusDefinition = "It lets you see which changes have been staged, which haven't, and which files aren't being tracked by Git. "

//CODE HERE

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

const gitAddDefinition = "Adds a change in the working directory to the staging area. It tells Git that you want to include updates to a particular file in the next commit."

const gitAddCode = "git add ."
//CODE HERE

//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

const gitCommitDefinition = "Captures a snapshot of the project's currently staged changes, or in other terms creates a 'save point.'"

const gitCommitCode = "git commit -m 'my commit'"

//CODE HERE


//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

const gitPushDefinition = "Send (or push) the commits from your local branch in your local Git repository to the remote repository(in our case github), opposite of fetch. "

//CODE HERE